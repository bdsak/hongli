name: Dividend Monitor

on:
  # UTC 07:40 = 北京 15:40（A股收盘后）
  schedule:
    - cron: '40 7 * * 1-5'

  # 手动运行
  workflow_dispatch:

  # 🔑 关键：用于激活 schedule（保留，不会影响正常使用）
  push:
    branches:
      - main

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run monitor
        env:
          SERVER_CHAN_KEY: ${{ secrets.SERVER_CHAN_KEY }}
        run: |
          python monitor.py
